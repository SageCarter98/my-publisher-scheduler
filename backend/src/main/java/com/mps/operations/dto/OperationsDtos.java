package com.mps.operations.dto; import java.time.Instant; import java.util.*; public final class OperationsDtos{private OperationsDtos(){} public record BackupView(UUID id,String backupType,String status,String storageLocation,String checksum,Long sizeBytes,String message,Instant requestedAt,Instant startedAt,Instant completedAt){} public record AlertView(UUID id,String alertType,String severity,String status,String title,String details,String source,Instant detectedAt,Instant acknowledgedAt,Instant resolvedAt){} public record HealthView(String status,boolean database,boolean documentStorage,long openAlerts,Instant checkedAt,Map<String,String> components){} }
